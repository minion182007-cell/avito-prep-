Group Anagrams — заметки

Идея. Анаграммы — это слова с одинаковым набором букв. Значит нужен канонический вид: представление, одинаковое у всех анаграмм и разное у остальных. Он и становится ключом мапы, а значением — список слов с таким ключом.

Приём называется группировкой по вычисляемому признаку и встречается постоянно: сгруппировать заказы по дню, пользователей по домену почты, файлы по хешу. Меняется только формула ключа.

Два способа канонизации:

Ключ	Сложность	Когда лучше
Отсортированное слово: eat → aet	O(n · k log k)	короче, читается сразу — выбор по умолчанию
Вектор из 26 частот с разделителем	O(n · k)	длинные слова, когда важна скорость

Здесь n — число слов, k — длина слова. Разделитель во втором способе обязателен: без него "11"+"1" и "1"+"11" дают одинаковое 111.

Что стоит запомнить про C++:

seen[key].push_back(word) работает, даже когда ключа ещё нет: operator[] создаёт значение по умолчанию — пустой vector<string> — и вставляет его. Обратная сторона: m[k] вставляет элемент даже при чтении. Для проверки наличия без вставки — m.count(k) или m.find(k).

for (auto& [key, vec] : seen) — structured bindings, распаковка пары в две переменные. С auto& работаешь со ссылкой на оригинал; с auto без амперсанда получишь копию каждого вектора.

При переносе в результат push_back(vec) копирует вектор целиком, push_back(move(vec)) переносит его внутренний буфер. Для больших групп разница заметна, и это ровно тот вопрос, который задают следующим на собеседовании.

Порядок групп не гарантирован — unordered_map обходится в произвольном порядке. Задача это допускает, но если бы требовался детерминированный вывод, понадобился бы map или сортировка результата.

Крайний случай: пустая строка даёт пустой ключ и попадает в собственную группу. Это корректно.
